#!/usr/bin/env ruby

require 'rubygems'
require 'daemons'

command = ARGV[0]
queue = ARGV[1]

puts "Starting lp-monitor with queue: #{queue}"

options = { :ARGV => [command, '--', queue]}

Daemons.run("lp-monitor", options)